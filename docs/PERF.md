# Замеры производительности

Каждый срез заканчивается замером. Число из замера нельзя ухудшать в следующих срезах.
Если число ухудшилось — сначала оптимизация, потом новые фичи.

> Все числа ниже сняты на машине владельца (железо в конце файла), сид 20260911, сборка Release.
> Симуляцию меряет отдельная программа без окна, кадры — сама игра по клавише F7.

## Как замерять

Симуляция без окна:

```
dotnet run -c Release --project src/WorldBox.Bench -- --ticks 5000 --size 512
```

Долгая история с эпохами и торговлей — проверка, что народы вообще развиваются:

```
dotnet run -c Release --project src/WorldBox.Bench -- --ticks 100000 --size 256 --tribes 7 --people 420
```

Замер печатает: время генерации карты, мс на тик, худший процент, тики в секунду, людей с рождениями и смертями,
народы и поселения, год и длину тика в годах, распределение эпох, состояние хозяйства (пути, перевозки,
голод, средние цены по товарам), контрольные суммы и готовую строку для таблицы ниже.
Первые 200 тиков идут на прогрев и в статистику не попадают.

Рендер и кадры: запустить игру и посмотреть оверлей в углу (F3 переключает).
Берём среднее за минуту спокойного наблюдения на скорости x1 и худший процент.

Списывать числа с экрана глазом не надо: клавиша **F7** дописывает текущий замер
в файл `frames.txt` в рабочей папке игры: окно, сцена (размер мира, режим карты, зум),
fps, среднее время кадра, худший процент, время симуляции, включённые слои
и готовая строка для таблицы кадров. Каждое нажатие — отдельная сцена в том же файле.
Важное о числах: в игре включена вертикальная синхронизация, поэтому fps упирается
в частоту экрана (60). Смотреть надо на время кадра и худший процент, а не на fps.

## Бюджет кадра при 60 fps

| Этап | Бюджет |
| --- | --- |
| Симуляция за кадр | 6 мс |
| Подготовка отрисовки | 3 мс |
| Рисование | 5 мс |
| Интерфейс | 2 мс |

Внутри бюджета симуляции у тяжёлых систем есть свои потолки:

| Система | Как часто | Потолок на один прогон |
| --- | --- | --- |
| Жители | каждый тик | 3 мс |
| Поселения | раз в 5 тиков | 2 мс |
| Хозяйство | раз в 5 тиков | 2 мс при 500 поселениях |
| Эпохи | раз в 10 тиков | 1 мс |
| Война | раз в 5 тиков | 1 мс при 256 отрядах |
| Общество | раз в 10 тиков | 1 мс |

## Симуляция (Bench)

| Дата | Срез | Сцена | мс/тик | худший 1% | Комментарий |
| --- | --- | --- | --- | --- | --- |
| 2026-09-11 | S0 | 512x512, пустой мир (`--tribes 0 --people 0`), 5000 тиков | 0.0068 | 0.0467 | цена самого цикла: людей и народов нет |
| 2026-09-11 | S5 | 512x512, 7 народов, 420 человек, 5000 тиков, 24 пути | 0.6075 | 0.7379 | 20 000 людей у потолка, 152 поселения, Бронзовый век |
| 2026-09-11 | S5 | 256x256, длинный прогон 100 000 тиков, 139 путей | 0.3789 | 0.4042 | мир доходит до Космоса, 12 морских путей |
| 2026-09-11 | S5 | 256x256, длинный прогон 100 000 тиков, 170 путей | 0.4026 | 0.4372 | после правки спроса на сырьё: нефть склад 588.6, уран 337.0, цены ушли с потолка |
| 2026-09-11 | S6 | 512x512, 7 народов, 420 человек, 5000 тиков, 35 путей | 0.6867 | 1.1033 | война включена: 10 войн идёт, 81 объявление, 71 замирение, 13 битв, 146 городов взято, 96 бунтов |
| 2026-09-11 | S6 | 256x256, длинный прогон 100 000 тиков, 152 пути | 0.6611 | 0.8899 | война включена: 4641 объявление, 481 битва, 3304 города взято, 2353 бунта, отрядов в пике 32 |
| 2026-09-12 | S7 | 512x512, 7 народов, 420 человек, 5000 тиков, 40 путей | 0.7025 | 0.8623 | общество включено: 18 вер из 48, 5 культур, один живой раскол, 458 обращений, 70 смут, стабильность 89 % |

Отдельной строки S4 нет нарочно: в замере нет флага, который отключал бы хозяйство,
поэтому люди + племена + эпохи живут внутри строки S5. Цена хозяйства видна отдельно в таблице систем ниже.

### Системы внутри замера S5 (512x512, 5000 тиков, 152 поселения)

| Система | Потолок | Последний прогон | Всего за замер |
| --- | --- | --- | --- |
| Жители | 3 мс | 0.706 мс | 2979.6 мс |
| Племена и поселения | 2 мс | 0.067 мс | 146.7 мс |
| Хозяйство | 2 мс при 500 поселениях | 0.025 мс | 43.7 мс |
| Эпохи | 1 мс | 0.018 мс | 25.6 мс |

Почти всё время тика съедают жители, и это ожидаемо: их всегда около 20 000 (потолок населения).
Поэтому пустой мир 512x512 и мир с семью народами отличаются почти в сто раз,
а размер карты сам по себе почти ничего не стоит.

### Системы внутри замера S6 (512x512, 5000 тиков, 173 поселения)

| Система | Потолок | Последний прогон | Всего за замер |
| --- | --- | --- | --- |
| Жители | 3 мс | 0.550 мс | 3060.8 мс |
| Племена и поселения | 2 мс | 0.054 мс | 186.6 мс |
| Земли | — | 0.033 мс | 190.7 мс |
| Дороги | — | 0.021 мс | 33.8 мс |
| Хозяйство | 2 мс при 500 поселениях | 0.032 мс | 52.8 мс |
| Эпохи | 1 мс | 0.023 мс | 34.8 мс |
| Война | 1 мс при 256 отрядах | 0.029 мс | 76.5 мс |

Война добавила к тику 0.08 мс (было 0.6075, стало 0.6867) — это около 1% бюджета симуляции.
Двести отрядов одновременно проверяются отдельно тестом `ArmyStoreHoldsTwoHundredArmiesAtOnce`:
в живом мире при текущих числах `data/war.json` их держится около двух десятков.

### Системы внутри замера S7 (512x512, 5000 тиков, 150 поселений)

| Система | Потолок | Последний прогон | Всего за замер |
| --- | --- | --- | --- |
| Жители | 3 мс | 0.497 мс | 3067.4 мс |
| Племена | 2 мс | 0.046 мс | 198.2 мс |
| Земли | — | 0.042 мс | 217.4 мс |
| Дороги | — | 0.011 мс | 33.7 мс |
| Хозяйство | 2 мс при 500 поселениях | 0.026 мс | 53.4 мс |
| Эпохи | 1 мс | 0.021 мс | 33.9 мс |
| Война | 1 мс при 256 отрядах | 0.169 мс | 77.8 мс |
| Общество | 1 мс | 0.024 мс | 19.9 мс |

Общество — самая дешёвая система в мире: 19.9 мс за весь замер против 3067 мс у жителей.
К тику оно добавило 0.016 мс (было 0.6867, стало 0.7025) — меньше трёх десятых процента бюджета симуляции.
Причина простая: прогон раз в 10 тиков, а внутри — проходы по спискам народов и поселений, а не по тайлам карты.

## Кадры (игра)

| Дата | Срез | Сцена | fps | мс/кадр | худший 1% | Комментарий |
| --- | --- | --- | --- | --- | --- | --- |
| | S0 | пустая сцена 1600x900 | | | | цель: 60 fps и меньше 4 мс |
| | S4 | 512x512, границы, поселения, панель народов | | | | цель: 60 fps, эпохи считаются раз в 10 тиков |
| | Арт | ближний план: декор, дома, таблички, тулбар | | | | самая тяжёлая сцена: зум 16–24 пикселя на тайл |
| | S5 | 512x512, окно торговли и линии путей | | | | цель: 60 fps при 512 путях на экране |

## На что смотреть в эпохах

Система эпох намеренно ленивая: она просыпается раз в 10 тиков и за один раз обходит только восьмую часть карты.
Полный круг по карте 512x512 — это 80 тиков, то есть четыре секунды на скорости x1.
Если `Эпохи` в построчной статистике замера стоят дороже 1 мс на тик, надо увеличивать число частей обхода,
а не оптимизировать внутренности.

## На что смотреть в хозяйстве

Хозяйство устроено так же: прогон раз в 5 тиков, народы обходятся восьмю частями, сеть путей пересобирается
раз в 400 тиков — именно эта пересборка и будет самым дорогим тиком в худшем проценте.
Бюджет из `docs/PLAN.md`: экономический прогон не дороже 2 мс при 500 поселениях. Замер печатает это число
отдельной строкой вместе с текущим числом поселений, чтобы сравнение было честным.
Что делать при превышении, по порядку:

1. Увеличить `SweepParts` в `EconomySystem` — народы будут обходиться мельче.
2. Увеличить `trade.rebuildTicks` в `data/economy.json` — сеть путей будет пересчитываться реже.
3. Уменьшить `trade.maxRoutes` — меньше путей и в симуляции, и в отрисовке.

Отрисовка путей живёт отдельно от симуляции: `TradeRenderer` молчит на дальнем зуме и отбрасывает пути за экраном,
поэтому его стоимость надо смотреть только в таблице кадров, а не в таблице симуляции.

## На что смотреть в войне

Война устроена так же лениво, как хозяйство: прогон раз в 5 тиков, границы осматриваются
восьмыми частями карты, причины войны проверяются раз в шесть прогонов.
Бюджет: военный прогон не дороже 1 мс при 256 отрядах. В замере на 100 000 тиков он стоит 0.045 мс:
отрядов в мире держится около десятка, потому что при текущих числах народ собирает войско
только с полутора тысяч жителей. Что делать при превышении, по порядку:

1. Уменьшить `armies.maxArmies` в `data/war.json` — меньше отрядов и в симуляции, и на экране.
2. Увеличить `SweepParts` в `WarSystem` — границы будут осматриваться мельче.
3. Увеличить `war.checkRuns` в `data/war.json` — объявления войны будут проверяться реже.

Если хочется, чтобы войск на экране было больше, менять надо не код, а `data/war.json`:
`armies.menPerArmy` — сколько людей в одном отряде, `armies.menPerThousandPeople` — сколько бойцов
народ снимает с тысячи жителей.

Городские амбары живут в хозяйстве и стоят один проход по списку поселений за прогон,
поэтому отдельной строки для них нет: они внутри числа «Экономический прогон».

## На что смотреть в обществе

Общество просыпается раз в 10 тиков и работает только со списками народов и поселений,
поэтому от размера карты почти не зависит. Смотреть надо не на время, а на числа блока «Общество» в замере:

- **вер ноль или под потолком 48** — сломано рождение вер: `religion.birthChancePerRun`, `religion.maxReligions`;
- **расколы есть, а живых расколов ноль** — молодые веры гаснут: поднимать `religion.youngRuns`;
- **смут ноль при средней стабильности 98–100 %** — власть держится сама собой: поднимать `stability.unrestWeight`
  или опускать `stability.base`;
- **смут сотни, стабильность ниже 50 %** — обратный случай: та же пара чисел в другую сторону;
- **обращений ноль** — вера не расходится: `religion.tradeSpreadChance`, `religion.neighbourSpreadChance`.

Всё это правится в `data/society.json` без пересборки кода.

## Железо владельца

| Что | Значение |
| --- | --- |
| Процессор | Intel Core i5-11400F, 6 ядер, 12 потоков, 2.6 ГГц |
| Память | 15.9 ГБ |
| Видеокарта | NVIDIA GeForce RTX 3050, 4 ГБ |
| Экран | 1920x1080 |
| Система | Windows 11, .NET SDK 8.0.425 |

Все числа в таблицах сняты на этой машине. На другом железе сравнивать их нельзя.

# Где мы сейчас

Файл обновляется после каждого среза. Закрыты срезы **S0–S4** (по коду), сверху лёг срез «Эпохи» (в `docs/PLAN.md` он значится как S4).

> Важное предупреждение: весь C# написан без компилятора — в песочнице агента нет ни .NET SDK, ни сети.
> Первый реальный `dotnet build` должен сделать владелец проекта; ожидаем порцию опечаток и правим их по списку ошибок.

## Что готово

### S0 — каркас
- Решение `WorldBox.sln`: `Core`, `Render`, `UI`, `Desktop`, `Bench`, `Tests`.
- Генератор случайных чисел `Rng` (xorshift128, потоки через `Fork`) — единственный источник случайности.
- Часы симуляции: фиксированный тик 20 Гц, скорости пауза/x1/x4/x16/x64, защита от отставания.
- Окно MonoGame, камера с плавным зумом, пиксельный шрифт без бинарных ассетов, отладочная панель.

### S1 — мир
- Генератор карты: высоты, температура, влажность, реки, плодородие, 19 биомов, 10 видов ресурсов.
- Шесть режимов карты, мини-карта с переходом по клику, легенда биомов, инспектор тайла.
- Отрисовка тайлов чанками 64x64 — одна текстура на чанк вместо сотен тысяч прямоугольников.

### S2 — тайлсет
- Процедурные тайлы 16x16, по 4 варианта на биом: генерируются кодом при загрузке, файлов с картинками нет.
- Кромки между биомами (берег, снег, песок) — переходы не выглядят резаными.
- Ближний план включается автоматически по зуму, аварийный выключатель — `F4`.

### S3 — жители
- До двадцати тысяч агентов в массивах (SoA), без объектов на человека.
- Голод и еда от плодородия тайла, теснота, миграция на соседний тайл, рождения, старость и смерть от голода.
- Сетка плотности по тайлам, счётчики жителей/рождений/смертей в панели, свечение скоплений на дальнем зуме.

### S4 — племена, поселения, границы
- `TribeStore`: до 64 народов, у каждого имя, цвет, родина, число жителей и поселений, текущая эпоха.
- Генератор русских имён народов и названий поселений (`TribeNames`) — имена уникальны в партии.
- `SettlementStore`: до 512 поселений трёх уровней — стоянка, деревня, город.
- `Territory`: владелец каждого тайла в одном массиве `short`, счётчики по народам и версия для кеша отрисовки.
- `SettlementSystem`: скопление людей на плодородной земле основывает стоянку, стоянка растёт в деревню и город, вокруг захватывается земля, опустевшие бросаются.
- `TribeSeeder`: стартовые очаги рассаживаются на суше с минимальным расстоянием друг от друга — народы не стартуют в одном комке.
- `TerritoryRenderer`: границы одной текстурой размером с карту, пересборка не чаще раза в 10 кадров и только при смене владельцев; край ярче заливки.
- Значки поселений масштабируются по уровню и не исчезают на дальнем зуме.
- В панели появилась строка «племён / поселений / крупнейшее».
- На старте: 7 народов по 60 человек.

### Эпохи (в `docs/PLAN.md` — S4)
- `data/eras.json` версии 2: одиннадцать эпох от палеолита до космоса. У каждой длина тика в годах, порог населения, нужные ресурсы и география, еда на работника, военная сила и загрязнение.
- `EraTable` читает файл целиком и проверяет его: порядок сверяется по полю `prev`, любая ошибка возвращается строкой на русском и не роняет игру — мир просто живёт без эпох, а причина печатается в консоль.
- `TribeTech` хранит на каждый народ прогресс исследований, свои и доступные через соседей ресурсы, географию владений, число тайлов, причину остановки, множитель еды и битовую маску соседей.
- `EraRules` отвечает на один вопрос: можно ли шагнуть в следующую эпоху, и если нет — чего именно не хватает (людей, ресурса, географии) или идёт ли тёмный век.
- `EraSystem` работает раз в 10 тиков и обходит карту полосами (восемь частей за полный круг): считает ресурсы и географию владений, ищет соседей по границе, копит прогресс, двигает эпохи, даёт подсмотреть знание у более развитого соседа и откатывает эпоху, если народ обезлюдел.
- Время сжимается: в палеолите тик — 25 лет, в космосе — четверть года. Прошлое не переписывается, прожитые годы только накапливаются.
- Еда зависит от эпохи: `PopulationSystem` умножает урожай тайла на `foodPerWorker` эпохи народа, поэтому неолит кормит заметно больше людей, чем палеолит.
- Отладочная панель (F3) показывает эпоху самого развитого народа и длину тика в годах.
- Клавиша `E` открывает панель народов: эпоха, люди, поселения, прогресс до следующей эпохи и причина остановки.
- Поселения выглядят по эпохе: хижина, частокол, башни с замком, дым мастерских, огни большого города (`EraStyle`, `TerritoryRenderer`).
- Инспектор тайла (F2) показывает владельца, его эпоху и ближайшее поселение с уровнем.
- Замер (`Bench`) считает полную симуляцию с людьми, племенами и эпохами: печатает распределение эпох, самую развитую эпоху, контрольную сумму и готовую строку для `docs/PERF.md`.
- Тесты `EraTests`: чтение настоящей таблицы, бронза без олова не наступает, народ доходит до новой эпохи и тик становится короче, отставший сосед учится через границу, один и тот же посев даёт одну и ту же историю.

## Чего ещё нет
- Войн, дипломатии и захвата чужих тайлов (пока занимается только свободная земля).
- Экономики: склады, цены, дефицит, торговые пути — это S5. Пока чужие ресурсы считаются доступными просто по общей границе.
- Спрайтов человечков с анимацией ходьбы и построек на тайлах (сейчас точки и квадратики).
- Инструментов бога, катастроф, религии, летописи, сохранений.
- Тёмные века есть, но поле `collapse.stabilityThreshold` пока не используется: откат эпохи считается только по нехватке людей.

## Как проверить

```bash
dotnet build
dotnet test
dotnet run --project src/WorldBox.Desktop
dotnet run -c Release --project src/WorldBox.Bench -- --ticks 5000 --size 512
dotnet run -c Release --project src/WorldBox.Bench -- --ticks 100000 --size 256 --tribes 7 --people 420
dotnet publish src/WorldBox.Desktop -c Release -r win-x64 --self-contained -p:PublishSingleFile=true
```

Что должно быть видно в окне: карта мира 512x512, семь цветных пятен владений с чёткой обводкой,
квадратики поселений, светлые точки людей на ближнем зуме и растущие числа в панели.
По клавише `E` — список народов с эпохой, прогрессом и причиной остановки.
В панели F3 — строки «эпоха» и «лет в тике»: пока все сидят в палеолите, тик равен 25 годам, и год в углу бежит быстро.
Длинный замер (`--ticks 100000`) должен показать в распределении эпох хотя бы неолит — если все одиннадцать тысяч тиков мир остался в палеолите,
значит цена исследований в `data/eras.json` завышена.

## Клавиши

| Клавиша | Действие |
| --- | --- |
| ПКМ / СКМ | панорама |
| Колесо | зум к курсору |
| WASD / стрелки | шаг камеры (Shift — быстрее) |
| ЛКМ по мини-карте | перейти в точку |
| M / Shift+M | следующий / предыдущий режим карты |
| T | границы и поселения |
| E | панель народов и эпох |
| L | легенда биомов |
| N | новый мир |
| R | тот же мир заново |
| Пробел | пауза |
| 1-4 | скорость x1 / x4 / x16 / x64 |
| Home | показать весь мир |
| F1 | подсказка снизу |
| F2 | инспектор тайла |
| F3 | отладочная панель |
| F4 | ближний план спрайтами |
| F5 | жители |
| Esc | выход |

## Риски
1. **Ни одна строка не компилировалась.** Сначала ждём список ошибок `dotnet build`, потом правим пачкой.
2. **Баланс жизни не проверен.** Население может вымереть или упереться в потолок 20 000 за минуты; цифры в `PopulationSettings` и `TribeSettings` правятся в одном файле.
3. **Баланс исследований не проверен.** Цена эпохи (`research.baseCost`, `research.costPerEra`) и шансы подсмотреть знание подобраны на глаз: народы могут застрять в палеолите навсегда или добежать до космоса за час. Правится в `data/eras.json` без пересборки кода.
4. **Сжатие времени влияет на всё, что считает года.** Часы симуляции остаются 20 Гц, меняется только цена тика в годах; рождаемость и голод нормируются через `PopulationSystem.ReferenceYears`, и при переходе в короткий тик эти множители надо перепроверить глазами.
5. **Соседи хранятся битовой маской `ulong`.** Больше 64 народов отследить нельзя (`TribeTech.MaxTracked`); если лимит народов вырастет, нужна другая структура.
6. **Отрисовка людей идёт по всем слотам каждый кадр.** При 20 000 агентах надо будет разбить по чанкам.
7. **2048x2048 включаем только после S5.** Сейчас базовый размер — 512x512.
